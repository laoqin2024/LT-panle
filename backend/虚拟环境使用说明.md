# Python虚拟环境使用说明

## 一、为什么需要虚拟环境？

### ✅ 虚拟环境的优势

1. **依赖隔离**
   - 不同项目可以使用不同版本的包
   - 避免包版本冲突
   - 保持系统Python环境干净

2. **项目可移植性**
   - 可以轻松复制项目到其他机器
   - requirements.txt记录所有依赖
   - 确保环境一致性

3. **团队协作**
   - 所有开发者使用相同的环境
   - 避免"在我机器上能运行"的问题

4. **生产部署**
   - 可以精确控制生产环境
   - 避免依赖冲突导致的问题

---

## 二、创建虚拟环境

### 方法1：使用脚本（推荐）

#### Linux/macOS
```bash
cd backend
bash setup_venv.sh
```

#### Windows
```cmd
cd backend
setup_venv.bat
```

### 方法2：手动创建

#### Linux/macOS
```bash
cd backend

# 创建虚拟环境
python3 -m venv venv-panle

# 激活虚拟环境
source venv-panle/bin/activate

# 升级pip
pip install --upgrade pip

# 安装依赖
pip install -r requirements.txt
```

#### Windows
```cmd
cd backend

# 创建虚拟环境
python -m venv venv-panle

# 激活虚拟环境
venv-panle\Scripts\activate.bat

# 升级pip
python -m pip install --upgrade pip

# 安装依赖
pip install -r requirements.txt
```

---

## 三、使用虚拟环境

### 激活虚拟环境

#### Linux/macOS
```bash
source venv-panle/bin/activate
```

#### Windows
```cmd
venv-panle\Scripts\activate.bat
```

激活后，命令行提示符会显示 `(venv-panle)` 前缀。

### 退出虚拟环境

```bash
deactivate
```

### 验证虚拟环境

```bash
# 检查Python路径
which python  # Linux/macOS
where python  # Windows

# 应该显示虚拟环境中的Python路径
# Linux/macOS: .../venv-panle/bin/python
# Windows: ...\venv-panle\Scripts\python.exe
```

---

## 四、日常开发流程

### 1. 启动开发

```bash
# 1. 进入项目目录
cd backend

# 2. 激活虚拟环境
source venv-panle/bin/activate  # Linux/macOS
# 或
venv-panle\Scripts\activate.bat  # Windows

# 3. 运行项目
uvicorn app.main:app --reload
```

### 2. 安装新包

```bash
# 激活虚拟环境后
pip install 包名

# 更新requirements.txt
pip freeze > requirements.txt
```

### 3. 更新依赖

```bash
# 激活虚拟环境后
pip install -r requirements.txt --upgrade
```

---

## 五、IDE配置

### VS Code

1. 打开项目文件夹
2. 按 `Ctrl+Shift+P` (Windows) 或 `Cmd+Shift+P` (macOS)
3. 输入 "Python: Select Interpreter"
4. 选择 `venv-panle/bin/python` (Linux/macOS) 或 `venv-panle\Scripts\python.exe` (Windows)

### PyCharm

1. File → Settings → Project → Python Interpreter
2. 点击齿轮图标 → Add
3. 选择 Existing environment
4. 选择 `venv-panle/bin/python` (Linux/macOS) 或 `venv-panle\Scripts\python.exe` (Windows)

---

## 六、常见问题

### Q1: 虚拟环境已创建，但激活失败

**解决方案**:
```bash
# 检查虚拟环境是否存在
ls venv-panle/  # Linux/macOS
dir venv-panle  # Windows

# 如果不存在，重新创建
rm -rf venv-panle  # Linux/macOS
rmdir /s venv-panle  # Windows
python3 -m venv venv-panle
```

### Q2: 安装包时权限错误

**原因**: 没有激活虚拟环境，安装到了系统Python

**解决方案**: 确保激活虚拟环境后再安装

### Q3: 虚拟环境占用空间太大

**原因**: 虚拟环境包含所有依赖包

**解决方案**: 
- 这是正常的，虚拟环境通常占用几百MB到几GB
- 可以使用 `pip list` 查看安装的包
- 可以删除不需要的包

### Q4: 如何删除虚拟环境

```bash
# 先退出虚拟环境
deactivate

# 删除目录
rm -rf venv-panle  # Linux/macOS
rmdir /s venv-panle  # Windows
```

---

## 七、最佳实践

### 1. 始终使用虚拟环境

- ✅ 每个项目使用独立的虚拟环境
- ✅ 不要使用系统Python直接安装包
- ✅ 团队协作时确保使用相同的虚拟环境

### 2. 管理依赖

- ✅ 使用 `requirements.txt` 记录所有依赖
- ✅ 安装新包后及时更新 `requirements.txt`
- ✅ 定期更新依赖包版本

### 3. 版本控制

- ✅ `.gitignore` 中已包含 `venv-panle/`
- ✅ 不要提交虚拟环境到Git
- ✅ 只提交 `requirements.txt`

### 4. 生产部署

- ✅ 在生产环境创建新的虚拟环境
- ✅ 使用 `requirements.txt` 安装依赖
- ✅ 使用固定版本号（可选，使用 `requirements-lock.txt`）

---

## 八、总结

### ✅ 必须使用虚拟环境

**原因**:
1. 依赖隔离，避免冲突
2. 项目可移植性
3. 团队协作一致性
4. 生产环境可控性

### 📋 快速参考

```bash
# 创建虚拟环境
python3 -m venv venv-panle

# 激活（Linux/macOS）
source venv-panle/bin/activate

# 激活（Windows）
venv-panle\Scripts\activate.bat

# 安装依赖
pip install -r requirements.txt

# 退出
deactivate
```

---

## 九、项目配置

本项目已配置：
- ✅ `.gitignore` 包含 `venv-panle/`
- ✅ `requirements.txt` 包含所有依赖
- ✅ 创建脚本 `setup_venv.sh` 和 `setup_venv.bat`

**建议**: 所有开发者都应该使用虚拟环境进行开发。

